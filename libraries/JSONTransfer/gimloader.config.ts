import { officialScriptConfig } from "$shared/config";

export default officialScriptConfig({
    input: "src/index.ts",
    name: "JSONTransfer",
    description: "A small library for downloading and uploading JSON.",
    author: "retrozy",
    version: "0.1.0",
    isLibrary: true
});
